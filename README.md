# RTMP Server

## Simple nginx rtmp server based on tiangolo/nginx-rtmp server

## Configuration
- verbose stdout logs
- hls (port 8080) playback

## Roadmap
- Provide stream key auth
- in browser viewer client
